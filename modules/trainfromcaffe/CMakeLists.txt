IF (NOT ENABLE_TRAINFROMCAFFE)
	SET(ENABLE_TRAINFROMCAFFE 0 CACHE BOOL "Enable training using caffe results")
ENDIF()

IF (ENABLE_TRAINFROMCAFFE)
	IF(DYNAPSEFX2)

		SET(CAER_COMPILE_DEFINITIONS ${CAER_COMPILE_DEFINITIONS} -DENABLE_TRAINFROMCAFFE=1 PARENT_SCOPE)
	
		SET(CAER_TFC_FILES modules/trainfromcaffe/trainfromcaffe.c)

		SET(CAER_C_SRC_FILES ${CAER_C_SRC_FILES} ${CAER_TFC_FILES} PARENT_SCOPE)
	ELSE()
		MESSAGE("NOT COMPILING HELLOWORLD AS IT DEPENDS ON THE DYNAPSEFX2 MODULE")
	ENDIF()	
ENDIF()
