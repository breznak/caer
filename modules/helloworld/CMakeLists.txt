IF (NOT ENABLE_HELLOWORLD)
	SET(ENABLE_HELLOWORLD 0 CACHE BOOL "Enable the hello world module")
ENDIF()

IF (ENABLE_HELLOWORLD)
	IF(DYNAPSEFX2)
		SET(CAER_COMPILE_DEFINITIONS ${CAER_COMPILE_DEFINITIONS} -DENABLE_HELLOWORLD=1 PARENT_SCOPE)

		SET(CAER_HW_FILES modules/helloworld/helloworld.c)

		SET(CAER_C_SRC_FILES ${CAER_C_SRC_FILES} ${CAER_HW_FILES} PARENT_SCOPE)
	ELSE()
		MESSAGE("NOT COMPILING HELLOWORLD AS IT DEPENDS ON THE DYNAPSEFX2 MODULE")
	ENDIF()
ENDIF()
