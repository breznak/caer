IF (NOT ENABLE_ROTATEFILTER)
	SET(ENABLE_ROTATEFILTER 0 CACHE BOOL "Enable the rotatefilter activity filtering module")
ENDIF()

IF (ENABLE_ROTATEFILTER)
	SET(CAER_COMPILE_DEFINITIONS ${CAER_COMPILE_DEFINITIONS} -DENABLE_ROTATEFILTER=1 PARENT_SCOPE)

	SET(CAER_RF_FILES modules/rotatefilter/rotatefilter.c)

	SET(CAER_C_SRC_FILES ${CAER_C_SRC_FILES} ${CAER_RF_FILES} PARENT_SCOPE)
ENDIF()
