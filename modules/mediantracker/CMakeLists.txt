IF (NOT ENABLE_MEDIANTRACKER)
	SET(ENABLE_MEDIANTRACKER 0 CACHE BOOL "Enable the mediantracker activity filtering module")
ENDIF()

IF (ENABLE_MEDIANTRACKER)
	SET(CAER_COMPILE_DEFINITIONS ${CAER_COMPILE_DEFINITIONS} -DENABLE_MEDIANTRACKER=1 PARENT_SCOPE)

	SET(CAER_MT_FILES modules/mediantracker/mediantracker.c)

	SET(CAER_C_SRC_FILES ${CAER_C_SRC_FILES} ${CAER_MT_FILES} PARENT_SCOPE)
ENDIF()
