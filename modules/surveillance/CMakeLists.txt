IF (NOT ENABLE_SURVEILLANCE)
	SET(ENABLE_SURVEILLANCE 0 CACHE BOOL "Enable the surveillance activity filtering module")
ENDIF()

IF (ENABLE_SURVEILLANCE)
	SET(CAER_COMPILE_DEFINITIONS ${CAER_COMPILE_DEFINITIONS} -DENABLE_SURVEILLANCE=1 PARENT_SCOPE)

	SET(CAER_SV_FILES modules/surveillance/surveillance.c)

	SET(CAER_C_SRC_FILES ${CAER_C_SRC_FILES} ${CAER_SV_FILES} PARENT_SCOPE)
ENDIF()
