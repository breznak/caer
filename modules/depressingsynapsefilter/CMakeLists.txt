IF (NOT ENABLE_DEPRESSINGSYNAPSEFILTER)
	SET(ENABLE_DEPRESSINGSYNAPSEFILTER 0 CACHE BOOL "Enable the depressing synapse filtering module")
ENDIF()

IF (ENABLE_DEPRESSINGSYNAPSEFILTER)
	SET(CAER_COMPILE_DEFINITIONS ${CAER_COMPILE_DEFINITIONS} -DENABLE_DEPRESSINGSYNAPSEFILTER=1 PARENT_SCOPE)

	SET(CAER_RF_FILES modules/depressingsynapsefilter/depressingsynapsefilter.c)

	SET(CAER_C_SRC_FILES ${CAER_C_SRC_FILES} ${CAER_DSF_FILES} PARENT_SCOPE)
ENDIF()
