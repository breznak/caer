IF (NOT ENABLE_GESTURELEARNINGFILTER)
	SET(ENABLE_GESTURELEARNINGFILTER 0 CACHE BOOL "Enable the gesture learning filter module")
ENDIF()

IF (ENABLE_GESTURELEARNINGFILTER)
	IF(DYNAPSEFX2)
		SET(CAER_COMPILE_DEFINITIONS ${CAER_COMPILE_DEFINITIONS} -DENABLE_GESTURELEARNINGFILTER=1 PARENT_SCOPE)

		SET(CAER_L_FILES modules/gesturelearningfilter/gesturelearningfilter.c)

		SET(CAER_C_SRC_FILES ${CAER_C_SRC_FILES} ${CAER_L_FILES} PARENT_SCOPE)
	ELSE()
		MESSAGE("NOT COMPILING HELLOWORLD AS IT DEPENDS ON THE DYNAPSEFX2 MODULE")
	ENDIF()	
ENDIF()
