IF (NOT ENABLE_LEARNINGFILTER)
	SET(ENABLE_LEARNINGFILTER 0 CACHE BOOL "Enable the learning filter module")
ENDIF()

IF (ENABLE_LEARNINGFILTER)
	IF(DYNAPSEFX2)
		SET(CAER_COMPILE_DEFINITIONS ${CAER_COMPILE_DEFINITIONS} -DENABLE_LEARNINGFILTER=1 PARENT_SCOPE)

		SET(CAER_L_FILES modules/learningfilter/learningfilter.c)

		SET(CAER_C_SRC_FILES ${CAER_C_SRC_FILES} ${CAER_L_FILES} PARENT_SCOPE)
	ELSE()
		MESSAGE("NOT COMPILING MEANRATEFILTER AS IT DEPENDS ON THE DYNAPSEFX2 MODULE")
	ENDIF()
ENDIF()
