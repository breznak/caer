IF (NOT ENABLE_MONITORNEUFILTER)
	SET(ENABLE_MONITORNEUFILTER 0 CACHE BOOL "Enable the mean rate filter module")
ENDIF()

IF (ENABLE_MONITORNEUFILTER)
	IF(DYNAPSEFX2)
		SET(CAER_COMPILE_DEFINITIONS ${CAER_COMPILE_DEFINITIONS} -DENABLE_MONITORNEUFILTER=1 PARENT_SCOPE)

		SET(CAER_MN_FILES modules/monitorneufilter/monitorneufilter.c)

		SET(CAER_C_SRC_FILES ${CAER_C_SRC_FILES} ${CAER_MN_FILES} PARENT_SCOPE)
	ELSE()
		MESSAGE("NOT COMPILING HELLOWORLD AS IT DEPENDS ON THE DYNAPSEFX2 MODULE")
	ENDIF()
ENDIF()
