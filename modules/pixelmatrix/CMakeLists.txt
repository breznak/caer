IF (NOT ENABLE_PIXELMATRIX)
	SET(ENABLE_PIXELMATRIX 0 CACHE BOOL "Enable pixel matrix module")
ENDIF()

IF (ENABLE_PIXELMATRIX)
	SET(CAER_COMPILE_DEFINITIONS ${CAER_COMPILE_DEFINITIONS} -DENABLE_PIXELMATRIX=1 PARENT_SCOPE)

	SET(CAER_PM_FILES modules/pixelmatrix/pixelmatrix.c)

	SET(CAER_C_SRC_FILES ${CAER_C_SRC_FILES} ${CAER_PM_FILES} PARENT_SCOPE)
ENDIF()
