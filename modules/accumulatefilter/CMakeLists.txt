IF (NOT ENABLE_ACCFILTER)
	SET(ENABLE_BAFILTER 0 CACHE BOOL "Enable the Accumulate filtering module")
ENDIF()

IF (ENABLE_ACCFILTER)
	SET(CAER_COMPILE_DEFINITIONS ${CAER_COMPILE_DEFINITIONS} -DENABLE_ACCFILTER=1 PARENT_SCOPE)

	SET(CAER_BA_FILES modules/accumulatefilter/accumulatefilter.c)

	SET(CAER_C_SRC_FILES ${CAER_C_SRC_FILES} ${CAER_BA_FILES} PARENT_SCOPE)
ENDIF()
